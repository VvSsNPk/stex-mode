# Emacs
*.elc
\#*\#
.\#*

# Nix build output symlinks
result
result-*

# vscode/ extension build artifacts and dependencies -- not source,
# regenerate with `npm install` / `npm run build` inside vscode/
vscode/*

# Unrelated local scratch file, not part of the project
main.txt
